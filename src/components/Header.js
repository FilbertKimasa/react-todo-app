import React from 'react';

function header() {
  return (
    <header>
      <h1>todos</h1>
      <p>Items will persist in the browser local storage</p>
    </header>
  );
}

export default header;
